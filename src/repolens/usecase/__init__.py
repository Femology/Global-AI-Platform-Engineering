"""Use-case layer.

Application-specific orchestration that coordinates domain entities to fulfil
RepoLens features (e.g. "analyze repository", "generate documentation").
Depends on ``domain`` only; talks to infrastructure through abstract ports.
"""
